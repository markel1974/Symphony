package stub

const Source5 = `
package main

import "fmt"

func main(a int, b int) {
	input := "INPUT"
	results := "RESULTS"
	z := a + b
	y := a - b
	x := a * b
	fmt.Println(input, a, b, results, z, z, y, x)
}
`
